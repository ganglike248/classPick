# /feedback 은 운영자 전용 관리 화면(비밀번호 로그인 후 피드백/랭킹 관리)이라
# 일반 방문자용 콘텐츠가 아니므로 크롤링 대상에서 제외한다.
#
# /register, /result 는 앱 흐름(설정 → 로그인 대기 → 수강신청) 안에서 상태를
# 이어받아야 실제 화면이 그려지지만, 상태 없이 접속해도 이제는 빈 화면이나
# 즉시 리디렉트가 아니라 "이 화면은 무엇이고 어떻게 들어오는지" 안내 콘텐츠를
# 보여준다. 색인은 필요 없어 각 페이지에서 meta robots=noindex 로 처리하고,
# 크롤러 접근 자체는 막지 않는다(광고 심사 크롤러 포함).

User-agent: *
Disallow: /feedback

# 애드센스/애드 크롤러는 명시적으로 전체 허용
User-agent: Mediapartners-Google
Allow: /

User-agent: AdsBot-Google
Allow: /

Sitemap: https://classpick.netlify.app/sitemap.xml
